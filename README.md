# bare-async
High-performance manual state machines and a naked async executor for systems-level Rust. Designed for bare-metal, embedded, and safety-critical applications where zero-allocation and no-std are hard requirements. Bypasses standard waker overhead for deterministic latency and explicit memory control. Build honest, auditable concurrency.
